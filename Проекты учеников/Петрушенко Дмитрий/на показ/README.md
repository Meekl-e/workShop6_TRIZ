# Репозиторий Петрушенко Дмитрия
## Структура данных
1. `load` нужен для облегчоной загрузки файлов
2. `изменение(открытие)` не работает без `изменение(создание)` так как открывать нечего (кучо ошибок)
 
3. остальные проекты в общее